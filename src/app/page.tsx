import { AuthForm } from "@/components/auth/AuthForm";

export default function HomePage() {
  return (
    <main className="family-bg flex min-h-[100dvh] items-center justify-center px-5 py-28">
      <AuthForm />
    </main>
  );
}
